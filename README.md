# federal_mbt
Автоматизированный Python-пайплайн для сбора, OCR-обработки и классификации PDF-документов о распределении федеральных бюджетных средств. Пайплайн работает в один запуск, автоматически обрабатывает данные за период пятница → пятница, ведёт логирование и легко адаптируется под другие регионы за счёт настраиваемых правил классификации.
# 📊 Автоматизированный пайплайн анализа распределения федерального бюджета

Данный репозиторий содержит **автоматизированный пайплайн** для:
- скачивания PDF-документов с государственного портала,
- OCR-распознавания текста,
- лемматизации (приведения слов к начальной форме),
- классификации документов,
- отбора материалов, связанных с распределением **федеральных бюджетных средств**.

Пайплайн запускается **одной командой**, автоматически обрабатывает данные **за период с пятницы по пятницу** и сохраняет результаты в папку соответствующей недели.

---

## 🧠 Общая логика работы

Пайплайн выполняет шаги **строго последовательно**:

1. `download_pdf.py`  
   Скачивание PDF-документов за заданный диапазон дат

2. `minepdf.py`  
   OCR-распознавание текста из PDF

3. `morphy.py`  
   Нормализация текста (лемматизация)

4. `classifer2.0.py`  
   Классификация и отбор целевых документов

5. `pipeline.py`  
   Оркестрация всех шагов + логирование

---

## 📁 Структура проекта

```text
project_root/
│
├── pipeline.py                  # Основной управляющий скрипт
├── download_pdf.py              # Загрузка PDF
├── minepdf.py                   # OCR
├── morphy.py                    # Лемматизация
├── classifer2.0.py              # Классификация
│
├── runs/
│   └── YYYY-MM-DD__YYYY-MM-DD/
│       ├── pipeline_YYYYMMDD_HHMMSS.log
│       ├── downloaded_documents/
│       ├── text_output_ocr/
│       ├── text_output_ocr_normalized/
│       ├── бюджетные_документы/
│       └── документы_цфо/
│
└── README.md

## ⚙️ Установка и зависимости
**Python**

Рекомендуется **Python 3.9+**

**Python-библиотеки**
```pip install requests tqdm pytesseract pdf2image pymorphy3

**Системные зависимости (Windows)**

**1. Tesseract OCR**
```https://github.com/UB-Mannheim/tesseract/wiki

Путь к tesseract.exe должен быть прописан в minepdf.py

**2. Poppler for Windows**
```https://github.com/oschwartz10612/poppler-windows

Путь к poppler/bin должен быть добавлен в PATH

# 🚀 Запуск пайплайна
**Автоматический режим (рекомендуется)**

Диапазон дат считается автоматически: **пятница → пятница**

```python pipeline.py --base-dir "D:\searchreferallaw"

**Ручное задание дат**
```python pipeline.py \
  --base-dir "D:\searchreferallaw" \
  --date-from 29.01.2026 \
  --date-to 05.02.2026

# 🗓 Автоматический запуск по расписанию (Windows)

Используйте **Планировщик заданий Windows:
**
Триггер: еженедельно → пятница

Действие:

Program/script: python
Arguments: pipeline.py --base-dir "D:\searchreferallaw"
Start in: D:\searchreferallaw

# 🧾 Логирование

Для каждого запуска создаётся лог-файл:

```pipeline_YYYYMMDD_HHMMSS.log


Лог содержит:
временные метки,
stdout и stderr всех шагов,
информацию об ошибках и времени выполнения.

# 🧩 Адаптация под другие регионы
**📌 Где настраивается классификация**

Все региональные и тематические правила находятся в файле:

```classifer2.0.py

**1️⃣ Ключевые слова бюджета**

Найдите список или словарь ключевых слов, например:

BUDGET_KEYWORDS = [
    "федеральный бюджет",
    "субсидия",
    "межбюджетный трансферт",
    "финансирование",
    "бюджетные средства"
]


Можно добавлять или удалять слова под нужную тематику.

**2️⃣ Региональные маркеры**

Пример для Центрального федерального округа:

REGION_KEYWORDS = [
    "ЦФО",
    "Центральный федеральный округ",
    "Москва",
    "Московская область"
]

**🔄 Для другого региона (пример: СЗФО)**
REGION_KEYWORDS = [
    "СЗФО",
    "Северо-Западный федеральный округ",
    "Санкт-Петербург",
    "Ленинградская область",
    "Карелия"
]

**3️⃣ Папки для результатов**

В classifer2.0.py задаются папки вывода:

BUDGET_OUTPUT_DIR = "бюджетные_документы"
REGION_OUTPUT_DIR = "документы_цфо"


Для другого региона:

REGION_OUTPUT_DIR = "документы_сзфо"

**4️⃣ Логика отбора**

Чаще всего используется правило:

if is_budget and is_region:
    ...

